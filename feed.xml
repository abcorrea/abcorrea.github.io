<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://abcorrea.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://abcorrea.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-22T08:47:28+00:00</updated><id>https://abcorrea.github.io/feed.xml</id><title type="html">blank</title><subtitle>Academic website of Augusto B. Corrêa </subtitle><entry><title type="html">Autoresearch to Optimize Powerlifted</title><link href="https://abcorrea.github.io/agentic-coding/2026/06/17/autoresearch-powerlifted.html" rel="alternate" type="text/html" title="Autoresearch to Optimize Powerlifted"/><published>2026-06-17T11:00:00+00:00</published><updated>2026-06-17T11:00:00+00:00</updated><id>https://abcorrea.github.io/agentic-coding/2026/06/17/autoresearch-powerlifted</id><content type="html" xml:base="https://abcorrea.github.io/agentic-coding/2026/06/17/autoresearch-powerlifted.html"><![CDATA[<p>All the cool kids these days try to use “<em>Autoresearch</em>” loops to let their coding agents run wild with their softwares. <a href="https://mrlab.ai/jendrik-seipp/">Jendrik Seipp</a> has successfully tried it out in some of his projects, and he borrowed me his handcrafted agentic skill to try on my own.</p> <p>My target was <a href="https://github.com/abcorrea/powerlifted">Powerlifted</a>. The rule was simple: make search faster <em>without changing what it does</em>. I used Claude Code with Opus 4.8. The entire setup and logs are <a href="https://github.com/abcorrea/powerlifted/tree/autoresearch/search-time-2026-06-07">available online</a>. This setup is entirely credited to Jendrik. The initial prompt was the following:</p> <div class="language-md highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Run autoresearch on this repo. The setup already exists — do not recreate
it.
<span class="p">
1.</span> Read <span class="sb">`autoresearch.md`</span> (the brain file), the tail of
   <span class="sb">`autoresearch.jsonl`</span>, and <span class="sb">`git log --oneline -20`</span>, then continue from
   wherever the loop left off.
<span class="p">2.</span> Loop forever: think → edit → <span class="sb">`./autoresearch.sh`</span> → decide with
   <span class="sb">`.claude/skills/autoresearch/scripts/decide.py`</span> → commit or revert →
   append one line to <span class="sb">`autoresearch.jsonl`</span> → repeat. Never stop to ask
   whether to continue; I will interrupt when I want to steer.
<span class="p">3.</span> Hard rules, non-negotiable:
<span class="p">   -</span> Behavior-preserving changes only. The harness enforces this via
     <span class="sb">`benchmarks/reference.json`</span>; never regenerate or edit that file, the
     suite, the harness scripts, or anything listed as off-limits in
     <span class="sb">`autoresearch.md`</span>.
<span class="p">   -</span> Keep a change only when <span class="sb">`decide.py`</span> says KEEP; revert on DISCARD,
     crash, or a <span class="sb">`./autoresearch.checks.sh`</span> failure, even if the numbers
     looked good.
<span class="p">   -</span> Strictly one planner execution at a time, and nothing else CPU-heavy
     while sweeps are being timed.
<span class="p">   -</span> Never use <span class="sb">`git clean`</span>. Revert with <span class="sb">`git checkout -- .`</span> and remove new
     files by hand.
<span class="p">4.</span> Update the <span class="gs">**What's Been Tried**</span> section of <span class="sb">`autoresearch.md`</span> every 5–10
   experiments so a future agent does not repeat dead ends.
</code></pre></div></div> <p>There is a brain file (<code class="language-plaintext highlighter-rouge">autoresearch.md</code>) that Claude reads and rewrites as it goes; a script that rebuilds the planner, runs a sanity check, and then benchmarks three runs over a fixed suite of 34 (config, task) pairs. The reference numbers are stored in <code class="language-plaintext highlighter-rouge">reference.json</code> where the we record, for every pair, the exit code, plan cost, expansions, and generations expected. Every sweep is checked against it, and any mismatch fails the run and reverts the change. On top of that, a small decision script compares the candidate’s timings against the current best and returns <code class="language-plaintext highlighter-rouge">KEEP</code>, <code class="language-plaintext highlighter-rouge">DISCARD</code>, or <code class="language-plaintext highlighter-rouge">RERUN</code>. Claude logs each experiment as one line in a ledger, commits the <code class="language-plaintext highlighter-rouge">KEEP</code>s, reverts the rest, and never stops to ask permission.</p> <p>I left it running for ~12 hours. I had to prompt it to continue twice, and ask it to correct a minor issue once (more on that below). But what did it actually do? Most of the experiments were discards, which is what I expected. Over about a dozen experiments it kept four, which fall into three ideas:</p> <ol> <li>It cut overhead in the grounder used by the delete-relaxation heuristics: a flat vector with a single scan instead of a hash map with a double lookup, reused buffers, hash tables cleared instead of reallocated.</li> <li>It removed redundant work in the successor generator. For example, once a join has enforced a static inequality precondition, every later join preserves it, so re-checking it after each join is useless. It now tracks which preconditions are already satisfied and skips the rechecks.</li> <li>It replaced small <code class="language-plaintext highlighter-rouge">std::vector</code>s with small-buffer-optimized vectors that keep their elements inline. (This is the <a href="https://www.boost.org/doc/libs/1_85_0/doc/html/boost/container/small_vector.html"><code class="language-plaintext highlighter-rouge">small_vector</code> from Boost,</a>.) The same idea applied twice: once in the weighted grounder, and once in the successor generator. These two were the biggest wins by far.</li> </ol> <p>(The small-vector work first used for <code class="language-plaintext highlighter-rouge">boost::container::small_vector</code>. But Boost is <a href="https://github.com/abcorrea/powerlifted/commit/3ad8a8148bf25fda862416e9477666a375c90cf5">no longer a Powerlifted dependency since a few months</a>, and I do not want it to become one again for a 200-line container. So I intervened and told Claude explicitly to not do so — and then I updated the prompt above to always mention that. Claude implemented a small, self-contained <code class="language-plaintext highlighter-rouge">small_vector</code> instead.)</p> <p>All of it was evaluated on a micro-suite on my laptop, so I ran a proper experiment with <a href="https://lab.readthedocs.io/en/stable/">lab</a>: a combination of IPC domains from the <a href="https://github.com/aibasel/downward-benchmarks/">downward-benchmarks</a> and the <a href="github.com/abcorrea/htg-domains/">hard-to-ground</a> suites. I used 1800 seconds and 16 GiB per task, which I always did when evaluating Powerlifted for papers, and compared the final commit against the baseline planner.</p> <p>The results are… quite good (gasp!). They are <a href="https://abcorrea.github.io/assets/tmp/autoresearch-powerlifted.html">available online</a>. On the instances solved by both (baseline and autoresearch version), search time dropped by 1.72x for GBFS with FF, for example. Coverage improved as a side effect in all tested configurations as well. No previously solved instance was lost.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/autoresearch-search-time-480.webp 480w,/assets/img/autoresearch-search-time-800.webp 800w,/assets/img/autoresearch-search-time-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/autoresearch-search-time.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/autoresearch-peak-memory-480.webp 480w,/assets/img/autoresearch-peak-memory-800.webp 800w,/assets/img/autoresearch-peak-memory-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/autoresearch-peak-memory.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> One configuration (<code>alt-bfws1</code>) as a representative example: per-task search time (left) and peak memory (right), with the baseline on the x-axis and the optimized branch on the y-axis. Search time sits below the diagonal (faster); peak memory sits a touch above it (the small price of inlining). The full numbers are in the <a href="https://abcorrea.github.io/assets/tmp/autoresearch-powerlifted.html">lab report</a>. </div> <p>So are these changes actually brand new? Yes and no. At the end, this <em>autoengineering</em> is not research. None of the changes is a completely new idea. For example, <a href="https://github.com/abcorrea/powerlifted/issues/23">Powerlifted has an open-issue since 6 years to optimize (in)equality checks</a>. But I never judged it worth doing. Luckily, Claude thought otherwise. Similarly, the small vector optimization is a known C++ optimization, and <a href="https://github.com/abcorrea/powerlifted/blob/af421dbfdb5514a2f1fb8db07ac5349481e7c25b/src/search/novelty/achieved_ground_atoms.h#L11">we even have a comment on it on the Powerlifted source code</a>. But, interestingly enough, both optimizations were applied in non-obvious ways: the inequality precondition issue had no concrete details to be implemented, ansd the small vector optimization was applied to a <em>different</em> part of the code than we had initially considered.</p> <p>Initially, I thought that Claude would conjure up lots of micro-optimizations that would sum up to a decent improvement. At the end, at least I have implementations of optimizations that I already wanted to do myself anyway. Everyone wins.</p>]]></content><author><name></name></author><category term="agentic-coding"/><category term="planning"/><category term="llms"/><category term="experiments"/><summary type="html"><![CDATA[An Autoresearch loop improved the performance of Powerlifted in 12 hours.]]></summary></entry><entry><title type="html">Translating Fast Downward’s Translator to C++ Using Claude Code</title><link href="https://abcorrea.github.io/agentic-coding/2026/06/02/claude-translator.html" rel="alternate" type="text/html" title="Translating Fast Downward’s Translator to C++ Using Claude Code"/><published>2026-06-02T11:00:00+00:00</published><updated>2026-06-02T11:00:00+00:00</updated><id>https://abcorrea.github.io/agentic-coding/2026/06/02/claude-translator</id><content type="html" xml:base="https://abcorrea.github.io/agentic-coding/2026/06/02/claude-translator.html"><![CDATA[<p>There was a lot of talk about the recent <a href="https://github.com/oven-sh/bun/pull/30412">Bun rewrite in Rust</a>. Anthropic <a href="https://www.anthropic.com/news/anthropic-acquires-bun-as-claude-code-reaches-usd1b-milestone">acquired Bun</a> at the end of last year, and recently announced that the rewrite from Zig to Rust was <a href="https://claude.com/blog/introducing-dynamic-workflows-in-claude-code">done using their new “dynamic workflow” orchestration feature</a>.</p> <p>This made me curious to see how Claude Code would do porting <a href="https://www.fast-downward.org/latest/">Fast Downward</a>’s translator from Python to C++. The implementation is not trivial, and the <a href="https://www.sciencedirect.com/science/article/pii/S0004370208001926">algorithms are quite sophisticated</a>. I have actually <a href="https://github.com/abcorrea/grounder">ported the grounder component to C++</a> during my Ph.D., so I had <em>some</em> experience with the same task. The code is <a href="https://github.com/abcorrea/downward-claude-translator">available online</a>.</p> <p>I forked Fast Downward and prompted Claude Code (with Opus 4.7 and high thinking effort) with the following instructions:</p> <div class="language-md highlighter-rouge"><div class="highlight"><pre class="highlight"><code>You are an expert systems programmer specialized in C++ and automated planning.

Your objective is to translate the Python "translator" component (@src/translate) of the Fast Downward planning system into high-performance, modern C++ (C++20 or later). The core goal of this translation is to eliminate Python overhead, optimize memory layouts, and speed up the PDDL-to-SAS+ translation phase.

The generated C++ code must match the styling, conventions, and architectural philosophy of Fast Downward's existing C++ search component (@src/search).

<span class="gu">## Testing and Evaluation Constraints</span>
When validating, testing, and benchmarking your C++ implementation, you must adhere to the following strict execution environment limits:
<span class="p">*</span> <span class="gs">**Time Limit:**</span> Exactly 120 seconds maximum per run.
<span class="p">*</span> <span class="gs">**Memory Limit:**</span> Exactly 2 GiB maximum per run.
<span class="p">*</span> <span class="gs">**Concurrency:**</span> At most 1 run can be executed at any given time (absolutely no parallelization or concurrent testing threads).
<span class="p">*</span> <span class="gs">**Test Data Location:**</span> You can find benchmark files to test and evaluate your translator implementation against under @misc/tests/benchmarks/.

<span class="gu">## Ready to Begin</span>
Acknowledge that you understand the task. Before writing any implementation code, we must go through a mandatory brainstorming to remove any ambiguity.
</code></pre></div></div> <p>Lots of things in the process were <em>ad hoc</em>, including the prompt. A few are worth explaining: I was running Claude Code in the background and I did not want it disturbing my work, so I limited it to 2 GiB per run and 120 seconds per task, without parallelism.</p> <p>In the brainstorming phase, Claude asked if it should convert file-by-file and then stop for testing, and what the workflow should look like. I told it to implement the entire translator in one shot and to use the following workflow:</p> <ol> <li>Claude implemented something and stopped whenever it believed to be done</li> <li>I evaluated the implementation on held-out benchmarks (i.e., a subset of IPC domains from the <a href="https://github.com/aibasel/downward-benchmarks/">downward-benchmarks repo</a>, and the <a href="https://github.com/abcorrea/htg-domains/">hard-to-ground domains benchmark</a>) on a cluster (using 8 GiB and 5 minutes per task)</li> <li>I gave the <a href="https://lab.readthedocs.io/en/stable/">lab</a> report back to Claude</li> <li>Claude tried to fix any unexpected behavior or performance problems</li> </ol> <p>(I used a single agent spawned from the terminal — no sub-agents, no orchestration, no <code class="language-plaintext highlighter-rouge">/goal</code>, no Ralph loop. The only global skill I have installed is the <a href="https://github.com/mitsuhiko/agent-stuff/tree/main/skills/uv"><code class="language-plaintext highlighter-rouge">uv</code> skill by Armin Ronacher</a> but this was only loaded later, when it created the lab scripts. Claude was also told to not change the algorithms conceptually.)</p> <p>During early development, Claude only had access to the small benchmark that is <a href="https://github.com/aibasel/downward/tree/main/misc/tests/benchmarks">shipped with Fast Downward</a>. Later, I augmented the benchmark suite from Fast Downward’s repo to include more challenging tasks, because Claude did not have enough tasks to estimate the performance of its own implementation.</p> <p>When it had problems, I tried to not tell Claude what it had done wrong or what should be fixed. Evidently, this is an extreme approach and it would not be what anyone would do in a real scenario. There were a few exceptions where I did have to intervene: a few times Claude was too defeatist (“<em>oh there is nothing we can do, we will never outperform the Python implementation</em>”) and I had to ask it things like “<em>What if you stop doing string comparisons and use indices instead?</em>”; on the other hand, Claude often decided to simply not implement certain ideas. For example, in the invariant synthesis algorithm, Claude ignored the variable ordering implemented in the translator (using the weighted causal graph) and went with something on its own — and it did not work properly. It took a few iterations until it finally implemented the same algorithm.</p> <p>The results are available online (for <a href="https://abcorrea.github.io/assets/tmp/claude-translator-ipc.html">the IPC</a> and for <a href="https://abcorrea.github.io/assets/tmp/claude-translator-htg.html">the hard-to-ground</a> domains). There are two columns per implementation: <code class="language-plaintext highlighter-rouge">-h2-lama-first</code> runs the full pipeline with invariant synthesis on; <code class="language-plaintext highlighter-rouge">-no-h2-lama-first</code> turns it off (<code class="language-plaintext highlighter-rouge">--invariant-generation-max-candidates 0</code>). When the invariant synthesis is off, the implementations produce the same task; when it is on, Claude failed to make the C++ implementation identical. The Mersenne Twister inside the invariant synthesis is deterministic on both sides: same seed (314159), same algorithm name. But <em>apparently</em> libstdc++’s <code class="language-plaintext highlighter-rouge">mt19937</code> plus <code class="language-plaintext highlighter-rouge">uniform_int_distribution</code> produces a different sequence than CPython’s <code class="language-plaintext highlighter-rouge">random.Random.randrange</code>. Claude never managed to find a workaround to make both identical.</p> <p>The speed improvement is quite substantial but, overall, I would expect that a good C++ port would give us a more robust improvement in number of solved tasks. The gain in coverage for the hard-to-ground benchmark was 12%, despite the substantial speed up in translation time. For the IPC domains, the coverage change is negligible. For context, our <a href="https://abcorrea.github.io/assets/pdf/correa-et-al-icaps2023.pdf">2023 paper</a> led to 7% gain in coverage, despite only reimplementing the grounder component.</p> <p>A few things never got fixed. The mismatch in the variable ordering in the invariant synthesis is the obvious one. And the significant memory improvements only happened after I explicitly suggested swapping string arguments for indices. Based on the conversation, Claude wouldn’t have proposed that refactor on its own.</p> <p>What about code quality? I would be lying if I said I read all the code. But the parts I’ve seen (mostly the <a href="https://github.com/abcorrea/downward-claude-translator/tree/main/src/translate-cpp/grounding">grounder component</a>) were more readable than I expected, although I would have done things differently in terms of data structures and data flow. The code seems to me a bit Pythonic sometimes, but this is perhaps an issue with my instructions.</p> <p>And how long did it take? Tough to say because this was not my priority, so often I left Claude hanging for a few hours. I also have Claude Code Pro, so the workload is “naturally” split into 5-hours chunks. If we assume that Claude was working on it from it the first commit until the last one in each session, then the entire project took around 26–27 hours (I am accounting some “buffer time” for the work done before the first commit, and also including the time taken for the benchmark evaluation).</p>]]></content><author><name></name></author><category term="agentic-coding"/><category term="planning"/><category term="llms"/><category term="experiments"/><summary type="html"><![CDATA[We translated Fast Downward's translator from Python to C++ using Claude Code.]]></summary></entry></feed>